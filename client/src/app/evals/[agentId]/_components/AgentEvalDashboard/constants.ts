/** Constants for AgentEvalDashboard. */
export const MIN_BATCHES_FOR_TREND = 2;
export const COMPARE_SELECTION_SIZE = 2;

/** Must track `styles.ts`'s `wrap.maxWidth` — passed to `LineChart`'s `w`
 *  prop so the trend chart fills the same content column as the metric
 *  cards above it, instead of vendor/ui's own 620px default. */
export const PAGE_MAX_WIDTH = 1080;
