import type { CSSProperties } from "react";

/** Co-located styles for EvalDashboard. */
export const s = {
  wrap: { display: "flex", flexDirection: "column", gap: 24, padding: "24px 32px", maxWidth: 1080, margin: "0 auto" } satisfies CSSProperties,
  headerRow: { display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 16 } satisfies CSSProperties,
  h1: { fontSize: 20, fontWeight: 700 } satisfies CSSProperties,
  subtitle: { fontSize: 13, color: "var(--text-secondary)", marginTop: 4 } satisfies CSSProperties,
  agentGrid: { display: "flex", flexDirection: "column", gap: 10 } satisfies CSSProperties,
  agentRow: {
    display: "flex",
    alignItems: "center",
    gap: 16,
    padding: "14px 16px",
    cursor: "pointer",
  } satisfies CSSProperties,
  agentIconBadge: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    width: 32,
    height: 32,
    borderRadius: 7,
    background: "var(--accent-bg)",
    flexShrink: 0,
  } satisfies CSSProperties,
  agentMainCol: { display: "flex", flexDirection: "column", gap: 3, flex: 1, minWidth: 0 } satisfies CSSProperties,
  agentNameRow: { display: "flex", alignItems: "center", gap: 8 } satisfies CSSProperties,
  agentName: { fontSize: 14, fontWeight: 600 } satisfies CSSProperties,
  agentIcon: { color: "var(--accent)" } satisfies CSSProperties,
  agentSubtitle: { fontSize: 12, color: "var(--text-muted)" } satisfies CSSProperties,
  agentSparkline: { flexShrink: 0 } satisfies CSSProperties,
  agentStats: { display: "flex", gap: 22, flexShrink: 0 } satisfies CSSProperties,
  agentStat: { display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 2, minWidth: 46 } satisfies CSSProperties,
  agentStatLabel: { fontSize: 10, fontWeight: 700, letterSpacing: 0.4, color: "var(--text-muted)" } satisfies CSSProperties,
  agentStatValue: (color: string): CSSProperties => ({ fontSize: 15, fontWeight: 700, color, fontVariantNumeric: "tabular-nums" }),
  agentChevron: { color: "var(--text-muted)", flexShrink: 0 } satisfies CSSProperties,
  metricCellConfigure: { fontSize: 13, color: "var(--accent)", minWidth: 70 } satisfies CSSProperties,
  sectionTitle: { fontSize: 13, fontWeight: 700, color: "var(--text-secondary)", marginBottom: 8 } satisfies CSSProperties,
  sectionTitleRow: { display: "flex", alignItems: "center", gap: 8, marginBottom: 8, color: "var(--text-secondary)" } satisfies CSSProperties,
  sectionTitleText: { fontSize: 13, fontWeight: 700, textTransform: "uppercase", letterSpacing: 0.4 } satisfies CSSProperties,
  noRuns: { fontSize: 13, color: "var(--text-muted)" } satisfies CSSProperties,
  tableWrap: {
    border: "1px solid var(--border)",
    borderRadius: 8,
    overflowX: "auto",
  } satisfies CSSProperties,
  table: {
    width: "100%",
    minWidth: 720,
    borderCollapse: "collapse",
    fontSize: 13,
    tableLayout: "fixed",
  } satisfies CSSProperties,
  th: {
    textAlign: "left",
    padding: "10px 14px",
    fontSize: 11,
    fontWeight: 700,
    letterSpacing: 0.4,
    textTransform: "uppercase",
    color: "var(--text-secondary)",
    borderBottom: "1px solid var(--border)",
    whiteSpace: "nowrap",
  } satisfies CSSProperties,
  thNum: {
    textAlign: "right",
    padding: "10px 14px",
    fontSize: 11,
    fontWeight: 700,
    letterSpacing: 0.4,
    textTransform: "uppercase",
    color: "var(--text-secondary)",
    borderBottom: "1px solid var(--border)",
    whiteSpace: "nowrap",
  } satisfies CSSProperties,
  tr: { borderBottom: "1px solid var(--border)" } satisfies CSSProperties,
  td: {
    padding: "10px 14px",
    verticalAlign: "middle",
  } satisfies CSSProperties,
  tdTruncate: {
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  } satisfies CSSProperties,
  tdNum: {
    padding: "10px 14px",
    textAlign: "right",
    fontVariantNumeric: "tabular-nums",
    whiteSpace: "nowrap",
  } satisfies CSSProperties,
  passBadge: {
    color: "var(--ok)",
    fontWeight: 600,
  } satisfies CSSProperties,
  failBadge: {
    color: "var(--crit)",
    fontWeight: 600,
  } satisfies CSSProperties,
  metricBarCell: { display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 6 } satisfies CSSProperties,
  metricBarTrack: { width: 40, height: 6, borderRadius: 3, background: "var(--bg-hover)", overflow: "hidden", flexShrink: 0 } satisfies CSSProperties,
  metricBarFill: (pctValue: number, color: string): CSSProperties => ({
    width: `${pctValue}%`,
    height: "100%",
    background: color,
    borderRadius: 3,
  }),
  metricBarValue: { fontVariantNumeric: "tabular-nums", minWidth: 32, textAlign: "right" } satisfies CSSProperties,
} as const;
