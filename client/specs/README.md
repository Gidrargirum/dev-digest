# client/specs/ — UI requirements

What the interface **must** do: screen states, error behaviour, acceptance
criteria for lesson screens.

| Specification | About |
|---|---|
| _(empty)_ | — |

## What belongs here

- Mandatory screen states: loading / empty / error / degraded.
- The error taxonomy: what surfaces as a toast, what inline, what full-screen
  (driven by `ApiError.status` from `src/lib/api.ts`).
- Acceptance criteria for new screens (L01–L08).

Read before adding a screen or changing error behaviour.
