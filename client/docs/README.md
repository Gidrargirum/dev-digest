# client/docs/ — how the studio works

Explanatory guides for web-layer mechanisms. One file per mechanism.

| Document | About |
|---|---|
| _(empty)_ | — |

## Candidates to write

`data-layer.md` (`api.ts` → hooks → TanStack Query cache invalidation) ·
`sse-live-log.md` (subscribing to `/runs/:id/events`, replaying the buffer) ·
`component-anatomy.md` (the component folder: styles/constants/helpers/index) ·
`ui-vendor.md` (what lives in `src/vendor/ui` and why it is vendored) ·
`i18n.md` (next-intl, `messages/<locale>`).

The route map already lives in [`../README.md`](../README.md) — link, don't restate.
