# e2e/docs/ — how a browser run works

Explanatory guides for the e2e layer. One file per mechanism.

| Document | About |
|---|---|
| _(empty)_ | — |

## Candidates to write

`flow-format.md` (the `*.flow.json` shape, `{BASE}` substitution, `assert`) ·
`hermetic-stack.md` (what `scripts/e2e.sh` brings up, and on which ports) ·
`debugging-failures.md` (screenshots in `test-results/`, common timeout causes).

> There is deliberately no documentation `specs/` here: the sibling `../specs/`
> holds **executable** `*.flow.json` flows. Written e2e specifications live in
> the root [`../../specs/`](../../specs/README.md).
