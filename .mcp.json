{
  "mcpServers": {
    "devdigest": {
      "command": "pnpm",
      "args": ["--dir", "mcp", "exec", "tsx", "src/server.ts"],
      "env": {
        "DEVDIGEST_API_URL": "http://localhost:3001"
      }
    }
  }
}
