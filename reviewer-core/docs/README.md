# reviewer-core/docs/ — how the engine works

Explanatory guides for the pure review core. One file per mechanism.

| Document | About |
|---|---|
| _(empty)_ | — |

## Candidates to write

`prompt-assembly.md` (section order, which slot is filled by what) ·
`injection-guard.md` (why one rule rather than denylists) ·
`grounding.md` (how citations are matched against the diff, why the score is
recomputed) · `structured-output.md` (Zod → JSON Schema, parse-with-repair,
retries) · `map-reduce.md` (when it kicks in, how partial Reviews are reduced).

The pipeline diagram already lives in [`../README.md`](../README.md) — link,
don't restate.
